services:
  app:
    build:
      context: . # Construye la imagen desde la carpeta actual (la raíz del proyecto)
      dockerfile: Dockerfile
    ports:
      - "5000:5000" 
    env_file:
      - .env 
    restart: unless-stopped